{
	// Place your midend 工作区 snippets here. Each snippet is defined under a snippet name and has a scope, prefix, body and 
	// description. Add comma separated ids of the languages where the snippet is applicable in the scope field. If scope 
	// is left empty or omitted, the snippet gets applied to all languages. The prefix is what is 
	// used to trigger the snippet and the body will be expanded and inserted. Possible variables are: 
	// $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. 
	// Placeholders with the same ids are connected.
	// Example:
	"func_expect": {
		"scope": "cpp",
		"prefix": "func_expect",
		"body": [
			"EXPECT_EQ(IRPrinter().print(func), R\"($1)\");",
			"$2"
		],
		"description": "Function Expect IR String"
	},
	"mod_expect": {
		"scope": "cpp",
		"prefix": "mod_expect",
		"body": [
			"EXPECT_EQ(IRPrinter().print(module), R\"($1)\");",
			"$2"
		],
		"description": "Module Expect IR String"
	},
	"print_func": {
		"scope": "cpp",
		"prefix": "print_func",
		"body": [
			"std::cout << IRPrinter().print(func) << std::endl;"
		],
		"description": "Print function IR"
	},
	"print_mod": {
		"scope": "cpp",
		"prefix": "print_mod",
		"body": [
			"std::cout << IRPrinter().print(module) << std::endl;"
		],
		"description": "Print module IR"
	}
}